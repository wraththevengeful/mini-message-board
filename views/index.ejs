<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
</head>

<body>
    <h1><%=title%></h1>
    <div>
        <% messages.map((message, index)=>{ %>
            <p>
                <%= message.text %>
            </p>
            <p>
                <%= message.user%>
            </p>
            <p>
                <%= message.added%>
            </p>
            <a href="/open/<%=index%>">Details</a>
            <% }) %>
    </div>

    <a href="/new">Add New Message</a>
</body>

</html>